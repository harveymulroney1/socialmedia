@{ ViewData["Title"] = "Home Page"; }

<div class="text-center">
    <h1 class="display-4">Create a new Post</h1>
    @using (Html.BeginForm("CreatePost", "Post", FormMethod.Post))
            {
            <div class=" = " form-group>
                <label for"PostCaption">Post Caption</label>
                <input type="text" name="PostCaption" value="" class="form-control" />
            </div>
            <div class=" = " form-group>
                <label for"PostText">Post Text</label>
                <input type="text" name="PostText" value="" class="form-control" />
            </div>
            <button class="btn btn-primary">Post</button>

            @Html.AntiForgeryToken();
            }

</div>
@*<a href="/Feed/Post">
    
    <button class="btn btn-primary">Post</button>

</a>*@